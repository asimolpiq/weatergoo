import 'package:flutter/material.dart';

class ProjectPaddings {
  final EdgeInsets minPadding = const EdgeInsets.all(5);
  final EdgeInsets midPadding = const EdgeInsets.all(10);
  final EdgeInsets maxPadding = const EdgeInsets.all(20);
  final EdgeInsets boxPadding = const EdgeInsets.symmetric(vertical: 30, horizontal: 5);
  final BorderRadius radius20 = BorderRadius.circular(20);
}
